---
- name: Create hosts file by templete
  hosts: nodes
  become: yes
  vars:
    nu: "{{ groups.nodes | count }}"

  tasks:
    - name: replace hosts file
      template:
        src: hosts.j2
        dest: /etc/hosts
